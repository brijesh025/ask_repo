## Comparison of structure between Node and Go implementations:
Node.js                         Go
------------------------------------------------
server.js / app.js              cmd/api/main.go or cmd/main.go
express.Router()                http.ServeMux / chi / gin router
routes/*.js                     internal/http/routes.go
controllers/*.js                internal/http/handlers.go
services/*.js                   internal/github, internal/search, etc.
models/*.js                     internal/models
db.js                           internal/storage
.env config                     internal/config

-------------------------------------------------------------Worker Directory Structure---------------------------------------------------
worker/
│
├── cmd/
│   └── worker/
│       └── main.go
│
├── internal/
│   ├── github/
│   ├── ingest/
│   ├── chunk/
│   ├── embed/
│   ├── storage/
│   ├── search/
│   ├── models/
│   ├── config/
│   └── utils/
│
├── tmp/
├── go.mod
└── go.sum

WHY? ------------->
  cmd: This directory contains the main entry point of the application. It is where the main.go file is located, which is the starting point of the worker application.
       it performs: 
        load config,
        initialize DB,
        initialize services,
        start indexing pipeline.

  internal: This directory contains the core logic and functionality of the worker application. It is organized into subdirectories based on different components or 
    features of the application, such as github, ingest, chunk, embed, storage, search, models, config, and utils. Each subdirectory contains code related to its specific
    functionality.

    1.github:clone repo,
            fetch branches,
            validate repo,
            GitHub API calls later.

    2.ingest: recursive file scanning,
            ignore rules,
            language detection,
            extracting clean files.

    3.chunk: chunking logic,
            line tracking,
            metadata generation.

    4.embed: embedding API calls,
            embedding batching,
            vector prepration
    
    5.storage: saving chunks,
            retrieving chunks,
            saving embeddings,
            repository metadata,
            retrieval quesries
    
    6.search: search logic,
            vector search,
            semantic retrieval,
            ranking

    7.models: shared data structures,
            database models

    8.config: DB URL,
            embedding API keys,
            temp directories
    
    9.utils:reusable helpers,
            file helpers,
            hash generation,
            logging utilities
  tmp: Temporary cloned repositories.


----------------------------------------------------------------Postgress SQL Database----------------------------------------------------------
  Set Up:
    1. Install PostgreSQL on your local machine or use a cloud-based PostgreSQL service.
    2. Create a new database named "askrepo".
    3. Create a new user named "askrepo" with password "askrepo" and grant all privileges on the "askrepo" database.

    SQL Commands:
    CREATE DATABASE askrepo;
    CREATE USER askrepo WITH PASSWORD 'askrepo';
    GRANT ALL PRIVILEGES ON DATABASE askrepo TO askrepo;

    --------> Local setup:
                Using Docker: 
                        docker run --name askrepo-postgres `
                        -e POSTGRES_USER=askrepo `
                        -e POSTGRES_PASSWORD=askrepo123 `
                        -e POSTGRES_DB=askrepo `
                        -p 5432:5432 `
                        -d postgres:16
                ->To check if the container is running:
                        docker ps
                ->To stop the container:
                        docker stop askrepo-postgres
                ->To remove the container:
                        docker rm askrepo-postgres
                ->To start the container again:
                        docker start askrepo-postgres
                ->After running the above command, you can connect to the PostgreSQL database using the following connection string:
                        DATABASE_URL=postgres://askrepo:askrepo123@localhost:5432/askrepo?sslmode=disable