env: "dev"

database: 
  url: "postgres://askrepo:Passit%401234@localhost:5432/askrepo?sslmode=disable"

local_storage:
  path: "E:/Working/AskRepo_Data/Repos"

http_server:
  address: "localhost:8080"
  timeout: 10s

cors:
  allowed_origin: "http://localhost:3000"

embedding:
  provider: "gemini"
  model: "gemini-embedding-001"
  dimensions: 1536

llm:
  provider: "gemini"
  model: "gemini-3.5-flash"
  temperature: 0.2
